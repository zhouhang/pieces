<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pieces.dao.CategoryMapper">
    <resultMap id="BaseResultMap" type="com.pieces.dao.model.Category">
            <id column="id"  property="id" />
            <result column="name"  property="name" />
            <result column="partenId"  property="partenId" />
            <result column="aliases"  property="aliases" />
            <result column="status"  property="status" />
            <result column="level"  property="level" />
            <result column="create_time"  property="createTime" />
        
    </resultMap>

    <sql id="Base_Column_List">
        id,name,partenId,aliases,status,level,create_time
    </sql>

    <select id="findById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from category
        where id = #{id}
    </select>

    <select id="findAll"  resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from category
    </select>

    <delete id="deleteById" parameterType="java.lang.Integer">
        delete from category
        where id = #{id}
    </delete>

    <insert id="create" parameterType="com.pieces.dao.model.Category">
        insert into
        category (name,partenId,aliases,status,level,create_time)
        values
        (#{name},#{partenId},#{aliases},#{status},#{level},#{createTime})
    </insert>

    <update id="update" parameterType="com.pieces.dao.model.Category">
        update category
        <set>
                                                        
            <if test="name != null">
                name = #{name},
            </if>
                                            
            <if test="partenId != null">
                partenId = #{partenId},
            </if>
                                            
            <if test="aliases != null">
                aliases = #{aliases},
            </if>
                                            
            <if test="status != null">
                status = #{status},
            </if>
                                            
            <if test="level != null">
                level = #{level},
            </if>
                                            
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
                            
        </set>
        where id = #{id}
    </update>
</mapper>